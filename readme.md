## side project
trying to get unrust at rust (pun intended), basically public styluaservice, with luau plugin,
publicly accessible, web service based 
since most plugins either compile rust --> wasm --> lua through Wasynth and pack each plugin distribution with stylua (super slow)
Or they require a external/local web server, making it super annoying to setup
if i just have a plugin with a public facing api, should be helpful for the community overall
bonus points i get to unrust
